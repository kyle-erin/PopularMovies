<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:buttonStyle">@style/MovieButton</item>
    </style>

    <style name="MovieButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:background">@color/button_color</item>
    </style>
</resources>
